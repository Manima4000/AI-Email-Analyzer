def preprocess_text(text: str) -> str:
    """
    Placeholder para o pré-processamento de texto.
    """
    print("Módulo NLP: Pré-processando o texto...")
    # No futuro, aqui entrará a remoção de stopwords, lematização, etc.
    return text.strip()